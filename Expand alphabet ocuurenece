#include<stdio.h>
#include<stdlib.h>

int main()
{
    char s[21];
    scanf("%s",s);
    int i,j,n;
    for(i=0;s[i]!='\0';i++){
        if(isdigit(s[i])){
            n=s[i]-'0';
            for(j=0;j<n;j++)
            printf("%c",s[i-1]);
        }
    }
}
